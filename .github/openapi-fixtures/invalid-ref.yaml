openapi: 3.0.3
info:
  title: Invalid reference fixture
  version: 1.0.0
paths:
  /widgets:
    get:
      operationId: listWidgets
      responses:
        "200":
          description: Widgets found.
          content:
            application/json:
              schema:
                $ref: "./missing.yaml#/components/schemas/Widget"
